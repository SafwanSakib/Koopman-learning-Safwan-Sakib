# Lightweight pip-installable subset of environment.yml, used only by CI
# (.github/workflows/ci.yml) for fast unit-test runs. Heavy/optional deps
# used only by specific baselines (do-mpc, casadi, wandb) are intentionally
# excluded here — those modules should mark their tests as skipped/xfail if
# the corresponding package is absent, rather than requiring it in CI.
numpy==1.26.4
scipy==1.13.0
matplotlib==3.8.4
pytest==8.1.1
pytest-cov==5.0.0
pyyaml==6.0.1
pandas==2.2.2
torch==2.3.0
cvxpy==1.5.1
osqp==0.6.7.post0
